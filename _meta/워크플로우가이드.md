# 스터디 워크플로우 가이드

## 최초 설정 (한 번만!)

### 1. Git 설치 확인
```bash
git --version
```
없으면 https://git-scm.com 에서 설치

### 2. 레포 클론
```bash
git clone https://github.com/selfishclub-all/aaa.git
cd aaa
```

### 3. Git 사용자 설정
```bash
git config user.name "본인닉네임"
git config user.email "본인이메일"
```

### 4. GitHub 인증 (둘 중 하나 선택)

**방법 A - Personal Access Token (추천)**
1. GitHub 로그인
2. Settings > Developer settings > Personal access tokens > Generate new token
3. repo 권한 체크 후 생성
4. push 할 때 비밀번호 대신 이 토큰 입력

**방법 B - SSH 키**
```bash
ssh-keygen -t ed25519
cat ~/.ssh/id_ed25519.pub
```
출력된 키를 GitHub > Settings > SSH and GPG keys > New SSH key에 추가

### 5. Obsidian에서 vault 열기
Obsidian > Open folder as vault > 클론한 aaa 폴더 선택

---

## 멤버가 하는 일

### 1. 미션 작성
- `00_주차별미션/Week_0N/` 에서 본인 파일 열기
- 템플릿 삽입: `Ctrl+P` → "템플릿 삽입" → `미션_Week_0N_닉네임` 선택
- 내용 작성 후 저장

### 2. 개념 정리 (선택)
- `01_개념/` 에 새 파일 만들기
- 파일명: `주제.md` (예: `AgentLoop개념.md`)
- 템플릿 삽입: `Ctrl+P` → "템플릿 삽입" → `개념_주제` 선택

### 3. 인사이트 작성 (선택)
- `02_인사이트/` 에 새 파일 만들기
- 파일명: `주제.md` (예: `멀티에이전트_활용법.md`)
- 템플릿 삽입: `Ctrl+P` → "템플릿 삽입" → `인사이트_주제` 선택

### 4. Push (터미널에서)
```bash
git pull origin main
git add .
git commit -m "Week_01 미션 제출 - 닉네임"
git push origin main
```

> 충돌이 나면 `git pull origin main` 먼저 하고 다시 push

---

## 템플릿 목록

| 템플릿 파일명 | 용도 | 누가 쓰나 |
| --- | --- | --- |
| `미션_Week_0N_닉네임.md` | 주차별 과제 작성 | 멤버 |
| `개념_주제.md` | 학습 개념 정리 | 멤버 |
| `인사이트_주제.md` | 생각/아이디어 정리 | 멤버 |
| `회의록_Week_0N.md` | 회의록 + MVP 투표 | 운영자 |
| `분석_Week_0N.md` | 통합 분석 리포트 | AI 자동 |

> Obsidian에서 `Ctrl+P` → "템플릿 삽입"으로 사용

---

## 폴더 안내

### 멤버가 직접 쓰는 폴더

| 폴더 | 설명 |
| --- | --- |
| `00_주차별미션/` | 주차별 과제 제출 |
| `01_개념/` | 배운 개념 정리 (주제명.md) |
| `02_인사이트/` | 생각/의견/아이디어 (주제명.md) |
| `03_회의/` | 회의록 + MVP 투표 |

### AI가 자동 생성하는 폴더 (건드리지 않아도 됨)

| 폴더 | 설명 |
| --- | --- |
| `_분석/` | 통합 분석 리포트 + 멤버별 프로필 |
| `_제안/` | 스킬 제안 + AAA 봇 인사이트 |
| `_아카이브북/` | 학습 아카이브 북 |
| `_현황/` | 출석/과제/벌금/링크드인/MVP |

---

## 벌금 규칙

| 항목 | 금액 |
| --- | --- |
| 출석 결석 1회 | 70,000원 |
| 과제 미제출 1회 | 70,000원 |

> `_현황/진행현황.md` 에서 누적 벌금 확인 가능

---

## Push 방법 (매번 이것만 하면 됨!)

### 작업 시작 전 - 최신 내용 받기
```bash
cd aaa
git pull origin main
```

### 작업 후 - 올리기
```bash
git add .
git commit -m "Week_01 미션 제출 - 닉네임"
git push origin main
```

### 예시
```bash
# 1주차 미션 제출
git pull origin main
git add .
git commit -m "Week_01 미션 제출 - 비비안"
git push origin main

# 개념 정리 추가
git pull origin main
git add .
git commit -m "AgentLoop 개념 정리 - 비비안"
git push origin main
```

### 에러가 나면?
```bash
# "rejected" 에러 → 최신 내용 먼저 받기
git pull origin main
git push origin main

# "conflict" 에러 → 충돌 해결 후 다시
git add .
git commit -m "충돌 해결"
git push origin main
```

---

## 운영자(다다)가 하는 일

멤버들이 push한 후 Claude Code에서 순서대로 실행:

```
/analyze 1        ← 1주차 통합 분석 + 벌금 계산 + 현황 업데이트
/propose          ← 프로젝트 제안 (필요할 때만)
/archive          ← 아카이브 북 갱신
```

그 후 결과를 커밋 & push:
```bash
git add .
git commit -m "Week_01 분석 완료"
git push origin main
```
